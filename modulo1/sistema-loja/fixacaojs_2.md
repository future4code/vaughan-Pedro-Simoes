``` function calculaPrecoTotal(quantidade) {
  // Escreva seu código aqui
  // se for menor que 11 ele vai pagar 1,30 por maça
  //se for maoir ou igual que 12 ele vai pagar 1 por cada maça 
    quantidade =Number(prompt("quantas maças voce quer"))
    if (quantidade < 11 ) {
    const resultado = quantidade * 1,30
    return resultado
    }else (quantidade => 12){
  
    const resultado2 =quantidade * 1
return resultado2
    }
      
    
      
}